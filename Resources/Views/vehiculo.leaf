<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalles del Vehículo</title>
    <!-- Incluir Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1>Detalles del Vehículo</h1>
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">{{ vehiculo.marca }} {{ vehiculo.modelo }}</h5>
                <p class="card-text">Número de ruedas: {{ vehiculo.num_ruedas }}</p>
                <p class="card-text">Tipo de combustible: {{ vehiculo.tipo_combustible }}</p>
                <p class="card-text">Pantalla central: {{ vehiculo.pantalla_central ? "Sí" : "No" }}</p>
                <p class="card-text">Tamaño de pantalla: {{ vehiculo.tamaño_pantalla }}</p>
            </div>
        </div>
    </div>
    <!-- Incluir Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
